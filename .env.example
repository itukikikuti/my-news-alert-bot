RSS_URLS=https://www.google.co.jp/alerts/feeds/xxxxx/yyyyy
# GUI_PORT=3334

# Web Push (VAPID) — generate keys with:
#   npx web-push generate-vapid-keys
# or:
#   node -e "import('web-push').then(wp => { const k = wp.default.generateVAPIDKeys(); console.log('VAPID_PUBLIC_KEY=' + k.publicKey); console.log('VAPID_PRIVATE_KEY=' + k.privateKey); })"
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:you@example.com
